source "https://rubygems.org"

# Pin to the github-pages gem to match the GitHub Pages build environment.
# Run: bundle install
# Preview: bundle exec jekyll serve --source docs --livereload
gem "github-pages", group: :jekyll_plugins

# Required for Ruby 3.x (WEBrick was removed from stdlib)
gem "webrick", "~> 1.8"
